# ============================================
#  GitHub Pages 배포용 - index.html 만 올림
#  나머지(버전본, 원본 이미지, CLAUDE.md 등)는
#  로컬 작업 파일이므로 저장소에 올리지 않음
# ============================================

# 우선 전부 제외
/*

# 배포에 필요한 파일만 다시 포함
!index.html
!.gitignore
